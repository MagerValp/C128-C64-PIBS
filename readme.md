Position Independent Basic Startup
==================================

Create a single prg that loads and runs machine language code on both C64 and C128.

Use `LOAD"*",8,1` on C64, use `DLOAD"*` on C128.
